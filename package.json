{
  "name": "gonsales",
  "version": "1.0.0",
  "description": "GoNSales static dashboard - development helper",
  "main": "index.html",
  "scripts": {
    "dev": "http-server -p 8000 -c-1",
    "dev-secure": "node security-headers.js",
    "build": "node deployment-cicd.js --build production",
    "build:staging": "node deployment-cicd.js --build staging",
    "build:production": "node deployment-cicd.js --build production",
    "deploy": "node deployment-cicd.js --deploy",
    "deploy:staging": "node deployment-cicd.js --deploy staging",
    "deploy:production": "node deployment-cicd.js --deploy production",
    "test": "node -e \"console.log('Run tests in browser: Open dev tools and run window.runAllTests()')\"",
    "test:performance": "node -e \"console.log('Performance tests available in browser console')\"",
    "test:accessibility": "node -e \"console.log('Accessibility tests available in browser console')\"",
    "monitor": "node -e \"console.log('Monitoring dashboard available in browser console')\"",
    "analytics": "node -e \"console.log('Analytics dashboard available in browser console')\"",
    "start": "npm run dev",
    "emulator:start": "firebase emulators:start --only firestore --project gonsales-emulator",
    "check": "node tools/check_page.js http://127.0.0.1:8000",
    "check:emulator": "npm run emulator:start"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "firebase-tools": "^12.11.0"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "firebase": "12.3.0",
    "jsdom": "^20.0.3"
  }
}
